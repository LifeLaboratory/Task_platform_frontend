/*
import {call, put, takeEvery} from 'redux-saga/effects'
import {push} from 'connected-react-router'
*/

/*
function* sampleSaga(action) {
    yield put({
      type: ACTION_TYPE
    })
}

*/


export default function* mySaga() {
  // yield takeEvery(actions.SAMPLE_ACTION, sampleSaga);
}